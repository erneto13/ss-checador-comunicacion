<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ChecadorComunicacion.ViewModels"
        mc:Ignorable="d" WindowState="Maximized" CanResize="False"
        WindowStartupLocation="CenterOwner"
        x:Class="ChecadorComunicacion.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="Hulla Pascal - Comunicación URN"
        Icon="../Assets/logo_pascal_huella.png">

    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <Grid RowDefinitions="Auto, Auto, *">
        <Grid Grid.Row="1" ColumnDefinitions="Auto, *">
            <Border Grid.Column="0" Width="100" Height="48">
            </Border>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="5" Margin="5" HorizontalAlignment="Center">
                <ListBox ItemsSource="{Binding Items}"
                         SelectedItem="{Binding SelectedListItem}"
                         Background="Transparent">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>

                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:ListItemTemplate}">
                            <StackPanel Spacing="15" Orientation="Horizontal">
                                <PathIcon Data="{Binding ListItemIcon}"
                                          Width="24" Height="24" />
                                <TextBlock Text="{Binding Label}"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                    <ListBox.ItemContainerTheme>
                        <ControlTheme TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="8,6" />
                            <Setter Property="Margin" Value="0,2" />
                            <Setter Property="CornerRadius" Value="4" />
                        </ControlTheme>
                    </ListBox.ItemContainerTheme>
                </ListBox>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Margin="5" Background="#2d2d2d">
            <TransitioningContentControl Content="{Binding CurrentPage}" />
        </Border>
    </Grid>

</Window>